# Pathsy
A React front\u2011end that lists tour packages fetched from the `/landing` API. It displays tour details (type, price, description, duration, availability, group type) and shows images from stored URLs. Built with React, Axios, and npm.
